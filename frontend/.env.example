# Copy this to .env.local for local overrides (never commit .env or .env.local)
# Set this as an Environment Variable in Vercel Dashboard → Project → Settings → Environment Variables

# URL of your deployed Spring Boot backend (no trailing slash)
# Example: https://carebridge-backend.up.railway.app
VITE_API_BASE=https://your-backend-server.com
